<%= form_for(@website) do |form| %>
  <p><%= form.label("title") %></p>
  <p><%= form.text_field("title") %></p>
  <p><%= form.label("url") %></p>
  <p><%= form.text_area("url") %></p>
  <p><%= form.submit %></p>
<% end %>
